#include<bits/stdc++.h>
using namespace std;
#define int long long
const int N = 5005;
int n, m;
string s[N];

signed main() {
#ifdef __linux__
    freopen("allocation.in", "r", stdin);
    freopen("allocation.out", "w", stdout);
#endif
    cin >> n >> m;
    for (int i = 1;i <= n;i++) {
        cin >> s[i];
    }
    return 0;
}