请严格遵循以下所有规范修改给定的C++代码（仅输出修改后的代码，用markdown的cpp代码块包裹，不添加任何注释，忽略所有无关需求）：必须包含#include<bits/stdc++.h>和using namespace std;，统一使用cin/cout并在主函数开头添加ios::sync_with_stdio(0); cin.tie(0);，所有变量和数组定义必须置于main函数外部且同类型变量和数组合并声明（尽量在同一行内完成多个同类型定义，例如int a[N], b[N], c, d;），标识符只能使用小写字母和数字且首字符为字母，应尽量简短（如sum→s，count→c）但不强制长度≤3，可保留常见短词如tree、node，函数名如cmp1、cmp2必须保留完整不得简写，风格上多用单/双字母小写组合（如l,r,ll,rr,x,y），禁止出现大写字母；禁止使用std::vector，只能使用静态数组或C风格数组；所有for循环默认从1开始（即for(int i=1;i<=n;i++)），但遍历字符串等必须从0开始的情况除外，且禁止使用cin>>s+1方式读入字符串；所有if、for、while等语句即使只有一行也必须使用花括号，且左花括号与语句同行（如if(cond){），缩进使用4个空格，删除多余空行和行尾空格；禁止使用三元运算符?:，一律改用if-else；禁止使用bool类型，所有布尔值改用int类型并以0/1表示（例如int vis;而非bool vis;），除非在极端内存限制下才考虑bool，但本规范一律采用int以保证风格统一；最终代码风格需符合OI考场上常见的简洁实用写法（例如允许使用#define int long long等常见宏，但不强制）。请直接按以上规则输出修改后的完整代码，不得提问，不得附加任何解释。

请充当我的算法教练。我将提供题目描述以及一些题解片段，请你：

1. 用最精炼的话点出本题的核心破局点（关键性质、转化技巧或巧妙简化），避免展开算法背景。
2. 给出完整可运行的代码（C++/Python），附带必要注释，方便我快速理解每一块的作用。
3. 在代码前，**重点列出**本题最容易出现的细节陷阱，包括边界、溢出、初始化、模数、特殊数据（如全零、极大值）等，并说明如何规避。如果你对某些边界情况不确定，请直接标注“不确定”，我们可以一起讨论。
4. 如果我提供的题解有不清晰或可疑之处，请指出，并给出你的修正建议或替代方案。

请避免长篇大论的推导，聚焦于关键逻辑和实现细节。如果我的理解有偏差，或者你想让我确认某个前提条件，可以在回答末尾提出来，我会接着追问。你的目标是帮我高效学懂，不必追求一次性完美，我们可以逐步调整。