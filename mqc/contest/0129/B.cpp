#include <bits/stdc++.h>
using namespace std;
#define int long long
const int N = 1e6 + 5;
int n, m;

struct node {
  int u, v, tim;
};

signed main() {
  cin >> n >> m;
  for (int i = 1; i <= m; i++) {
    int x, y;
    cin >> x >> y;
  }
  for (; q--;) {
    int l, r;
    cin >> l >> r;
  }
  return 0;
}