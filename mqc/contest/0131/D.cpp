#include <bits/stdc++.h>
using namespace std;
#define int long long
const int N = 1e6 + 5;
const int mod = 998244353;
int q, n, A, B, C, a, b, c;

struct node {
  int k;
  string s;
} k[N];

signed main() {
  cin >> n >> q >> A >> B >> C >> a >> b >> c;
  for (int i = 1; i <= n; i++) {
    cin >> k[i].k >> k[i].s;
  }
  for (; q--;) {
  }
  return 0;
}