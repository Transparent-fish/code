#include <bits/stdc++.h>
using namespace std;
#define int long long
const int N = 1e6 + 5;
int n, c, w[N], v[N], k[N];

signed main() {
    cin >> n >> c;
    for (int i = 1;i <= n;i++) {
        cin >> w[i] >> v[i] >> k[i];
    }
    return 0;
}